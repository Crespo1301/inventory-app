/** App-level metadata used across screens (support + legal links). */

/** Support email shown in the Account screen and used for mailto links. */
export const SUPPORT_EMAIL = 'crespo.csolutions@gmail.com';

/**
 * Public URL of the hosted Privacy Policy.
 *
 * PLACEHOLDER — the policy in `docs/privacy-policy.md` is written but not yet
 * hosted. Update this constant to the live HTTPS URL once the policy is
 * published, and paste the same URL into App Store Connect.
 */
export const PRIVACY_POLICY_URL = 'https://carloscrespo.info/kitchen-inventory/privacy';
